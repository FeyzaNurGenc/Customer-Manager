@model Customer_manager.Models.Customer_infos




@*  tabloları buraya koyabiliriz  *@
<div class="row">
    <div class="col-lg-12">

        <div class="card">
            <div class="card-body">
                <h5 class="card-title">Portfolyo</h5>

                <!-- Default Table -->
                <table class="table">
                    <thead>
                    <th>@Html.ActionLink("Yeni Portfolyo Ekle", "Create", "Portfolyo", null, new { @class = "btn btn-success", role = "button" })</th>

                    <tr>


                        <th scope="col">Kategori</th>
                        <th scope="col">Proje Adı</th>
                        <th scope="col">Fotoğraf</th>
                        <th scope="col">Yazı</th>
                        <th scope="col">İşlemler</th>


                    </tr>
                    </thead>
                    <tbody>
                        @foreach (var i in Model.Portfolyo)
                        {
                            <tr>

                                <td>@i.Category</td>
                                <td>@i.ProjectName</td>
                                <td>@i.PhotoPath</td>
                                <td>@i.Descripton</td>

                                <td>
                                    <div style="display: inline-block;">
                                        <a href="/Portfolyo/edit/@i.ID" class="btn btn-success">Güncelle</a>
                                    </div>
                                    <div style="display: inline-block;">
                                        <form id="deleteForm" action="/Portfolyo/delete/@i.ID" method="post">
                                            <button class="btn btn-danger" type="submit" onclick="return confirm('Silmek İstediğininze Emin misiniz?')">Sil</button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        }



                    </tbody>
                </table>

                <!-- End Default Table Example -->
            </div>
        </div>

    </div>
</div>

